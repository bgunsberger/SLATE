{
  "name": "scope-framework",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check reference/semantics.js && node --check reference/validation.js && node --check scripts/validate-spec.js",
    "validate:spec": "node scripts/validate-spec.js && node --test tests/spec.test.js"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
